export interface Why {
    title: string
    desc: string
    image: string
    titleColor: string
}

export interface Position {
    title: string
    department: string
    type: string
    location: string
}

export interface Value {
    title: string
    description: string
    icon: string
}